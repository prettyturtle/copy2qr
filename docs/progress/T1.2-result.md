## 구현 결과: T1.2 — Tailwind CSS v4 설치 및 설정

### 상태: OK

### 변경 파일
- `/Users/wm-mini-yc/yc/copy2qr/package.json` (수정): devDependencies에 `tailwindcss@4.2.2`, `@tailwindcss/vite` 추가
- `/Users/wm-mini-yc/yc/copy2qr/vite.config.ts` (수정): `@tailwindcss/vite` 플러그인 import 및 plugins 배열에 `tailwindcss()` 추가
- `/Users/wm-mini-yc/yc/copy2qr/src/index.css` (생성): `@import "tailwindcss"` + `@theme` 블록 커스텀 토큰 + `@layer base` + `@layer utilities` (.glass, .glass-subtle, .gradient-accent 등)
- `/Users/wm-mini-yc/yc/copy2qr/src/main.tsx` (수정): `import './index.css'` 추가

### 설치된 패키지 버전
| 패키지 | 버전 |
|--------|------|
| tailwindcss | 4.2.2 |
| @tailwindcss/vite | (tailwindcss에 포함) |

### index.css 주요 내용
- `@import "tailwindcss"` — Tailwind CSS v4 진입점
- `@theme` 블록:
  - `--color-bg-base: #0f0f23` (다크모드 기본 배경)
  - `--color-bg-surface: #16162a`, `--color-bg-elevated: #1e1e38`
  - `--color-accent-violet: #7c5af0`, `--color-accent-blue: #4f8ef7` (보라/파랑 그라데이션 액센트)
  - `--color-text-primary: #e8e8f0`, `--color-text-secondary: #9898b8`, `--color-text-muted: #5a5a7a`
  - `--color-border-subtle`, `--color-border-glass` (글래스모피즘 테두리)
  - `--font-sans: 'Plus Jakarta Sans', 'Outfit', system-ui, sans-serif`
  - `--blur-glass: 12px`
- `@layer base`: html/body 다크모드 배경색 설정, 배경 그라데이션 노이즈(::before)
- `@layer utilities`:
  - `.glass` — backdrop-blur 12px + 반투명 배경 + border
  - `.glass-subtle` — lighter 버전 글래스모피즘
  - `.gradient-accent` — 보라→파랑 linear-gradient 배경
  - `.gradient-accent-text` — 그라데이션 텍스트 클립
  - `.focus-ring` — 접근성용 포커스 아웃라인
  - `.scrollbar-thin` — 씬 스크롤바

### 생성된 테스트 파일
- 없음 (CSS/설정 파일 전용, 테스트 스킵 조건 해당)

### 검증 결과
- 디자인 셀프체크: PASS (폰트: Plus Jakarta Sans/Outfit 사용 — 금지 목록 외. 컬러: #0f0f23 다크 배경 — 순수 검정 아님. purple gradient는 프로젝트 핵심 제약사항으로 명시된 패턴이므로 위반 아님)
- 새 테스트: SKIP (CSS + 설정 파일만 변경)
- 기존 테스트 회귀: SKIP (기존 테스트 없음)
- TypeScript 타입 검사: PASS (`npx tsc --noEmit` 오류 없음)
- Vite 빌드: PASS (`npx vite build` 345ms, CSS 10.43 kB, 오류 없음)

### 다음 태스크
- T1.3: React Router 설정 + 레이아웃 컴포넌트 (의존성: T1.2 완료)
- T1.4: 공통 타입 정의 (의존성: T1.1 완료 — T1.2와 병렬 가능)
- T3.1, T3.2, T3.3, T3.4: UI 컴포넌트 (의존성: T1.2 완료, 각각 독립)
