## 구현 결과: T1.4 — 공통 타입 정의

### 상태: OK

### 변경 파일
- `/Users/wm-mini-yc/yc/copy2qr/src/types/index.ts` (생성): `DataType`, `PasteData`, `SharePayload`, `ImageSizeOption` 타입 export

### 구현 내용
```ts
export type DataType = "text" | "url" | "image";

export interface PasteData {
  type: DataType;
  content: string;
  timestamp: number;
}

export interface SharePayload {
  data: string;
  type: DataType;
}

export type ImageSizeOption = "resize" | "preview-only";
```

### 생성된 테스트 파일
- 없음 (순수 타입 정의 파일 — 실행 로직 없음, 테스트 스킵 조건 해당)

### 검증 결과
- 디자인 셀프체크: SKIP (비프론트엔드 — UI 없는 타입 선언 파일)
- 새 테스트: SKIP (테스트 미생성)
- 기존 테스트 회귀: SKIP (기존 테스트 없음)
- TypeScript 타입 검사: PASS (`npx tsc --noEmit` 오류 없음)

### 다음 태스크
- T2.1: 타입 감지 유틸리티 (의존성: T1.4 완료)
- T2.3: 이미지 처리 유틸리티 (의존성: T1.4 완료)
- T2.5: useShareData 훅 (의존성: T2.2, T1.4)
