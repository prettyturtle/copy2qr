## 구현 결과: T2.1 — 타입 감지 유틸리티

### 상태: OK

### 변경 파일
- `src/utils/detect-type.ts`: `detectType(content: string): DataType` 함수 생성. `/^https?:\/\//i` 정규식으로 URL 판별, 나머지는 text 폴백.

### 생성된 테스트 파일
- 없음 (테스트 프레임워크 미설치 — Vitest/Jest 없음, 테스트 스킵)

### 검증 결과
- 디자인 셀프체크: SKIP (비프론트엔드)
- 새 테스트: SKIP (테스트 미생성)
- 기존 테스트 회귀: SKIP (기존 테스트 없음)
- TypeScript 타입 체크: PASS (`npx tsc --noEmit` 오류 없음)

### 다음 태스크
- T2.2 (compress.ts) — 독립적, 즉시 실행 가능
- T2.3 (image.ts) — 독립적, 즉시 실행 가능
