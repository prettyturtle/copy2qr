## 구현 결과: T2.3 — 이미지 처리 유틸리티

### 상태: OK

### 변경 파일
- `src/utils/image.ts` (신규 생성): `fileToBase64`, `resizeImage`, `isImageOversized`, `getCompressedImageSize` 4개 함수 export

### 구현 내용 요약
- `fileToBase64(file: File): Promise<string>` — FileReader API로 File → base64 data URL 변환
- `resizeImage(base64, maxWidth, maxHeight, quality): Promise<string>` — canvas 기반 비율 유지 리사이즈, 최대 300x300 클램프, JPEG toDataURL 출력
- `isImageOversized(base64, maxBytes): boolean` — base64 데이터 길이로 원본 바이트 추정 (`(len * 3) / 4`), maxBytes 초과 여부 반환
- `getCompressedImageSize(base64): number` — lz-string `compressToEncodedURIComponent` 압축 후 예상 URL 길이 반환

### 핵심 제약사항 준수
- Arrow function 전용 사용 ✅
- 이미지 리사이즈 최대 300x300 (`Math.min(maxWidth, 300)`, `Math.min(maxHeight, 300)`) ✅
- JPEG 품질 조절 파라미터 `quality` (0~1, 클램프 처리) ✅

### 생성된 테스트 파일
- 없음 (테스트 프레임워크 미설치 — package.json에 jest/vitest 없음, 스킵)

### 검증 결과
- 디자인 셀프체크: ⏭️ SKIP (비프론트엔드 로직)
- 새 테스트: ⏭️ SKIP (테스트 프레임워크 없음)
- 기존 테스트 회귀: ⏭️ SKIP
- TypeScript 타입 체크: ✅ PASS (tsc --noEmit 오류 없음)

### 다음 태스크
- T2.4 (useClipboard) — T2.1, T2.3 완료 후 실행 가능
