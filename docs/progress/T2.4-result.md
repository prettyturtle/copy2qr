## 구현 결과: T2.4 — useClipboard 훅

### 상태: OK

### 변경 파일
- `src/hooks/useClipboard.ts` (신규 생성): `useClipboard` 훅 export. `UseClipboardOptions` 인터페이스 export.

### 구현 내용 요약
- `useClipboard({ onPaste }: UseClipboardOptions): void`
  - `useEffect`로 `document`에 `paste` 이벤트 리스너 등록, cleanup에서 `removeEventListener` 호출
  - `handlePaste`는 `useCallback`으로 메모이제이션 (onPaste 의존)
  - `clipboardData.items` 순회 전략:
    1. `image/*` MIME 아이템 우선 탐색 → `getAsFile()` → `new File([blob], ...)` → `fileToBase64()` → `type: "image"` PasteData 콜백 호출 후 return
    2. 이미지 없으면 `text/plain` 아이템 → `getAsString()` → `detectType()` → `type: "text" | "url"` PasteData 콜백 호출
  - 이미지 변환 실패는 try/catch로 무시 (paste 이벤트 흐름 중단 방지)
  - `content`가 빈 문자열이면 콜백 호출 안 함

### 핵심 제약사항 준수
- Arrow function 전용 (`useClipboard`, `handlePaste` 모두 const arrow function) ✅
- paste 이벤트 기반 구현 (Clipboard API 미사용) ✅
- useEffect cleanup 포함 ✅

### 생성된 테스트 파일
- 없음 (테스트 프레임워크 미설치 — package.json에 jest/vitest 없음, 스킵)

### 검증 결과
- 디자인 셀프체크: SKIP (클라이언트 로직, UI 변경 없음)
- 새 테스트: SKIP (테스트 프레임워크 없음)
- 기존 테스트 회귀: SKIP (기존 테스트 없음)
- TypeScript 타입 체크: PASS (`npx tsc --noEmit` 오류 없음)

### 다음 태스크
- T4.1 (PasteZone 컴포넌트) — T2.4, T3.2 완료 후 실행 가능
