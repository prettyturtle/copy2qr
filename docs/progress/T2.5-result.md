## 구현 결과: T2.5 — useShareData 훅

### 상태: OK

### 변경 파일
- `src/hooks/useShareData.ts`: `useShareData` 훅 신규 생성.
  - `encodeShareUrl(content, type)`: `buildShareUrl` 호출, `useCallback` 적용
  - `decodeShareData()`: `window.location.hash`에서 `URLSearchParams`로 query params 파싱, `decompressData`로 복원, 실패 시 `null` 반환, `useCallback` 적용
  - `copyShareLink(url)`: `navigator.clipboard.writeText` 래핑, `useCallback` 적용
  - 반환값: `{ encodeShareUrl, decodeShareData, copyShareLink }`

### 생성된 테스트 파일
- 없음 (테스트 프레임워크 미설치 — Vitest/Jest 없음, 테스트 스킵)

### 검증 결과
- 디자인 셀프체크: SKIP (UI 변경 없는 클라이언트 로직)
- 새 테스트: SKIP (테스트 미생성)
- 기존 테스트 회귀: SKIP (기존 테스트 없음)
- TypeScript 타입 체크: PASS (`npx tsc --noEmit` 오류 없음)

### 호출처 영향
- 신규 파일 생성이므로 기존 호출처 없음.
- 향후 사용처: T4.4 (ActionBar), T4.5 (HomePage), T5.2 (SharePage)

### 다음 태스크
- T2.6 (useToast) — 이미 완료 (✅)
- T4.4 (ActionBar) — T3.1, T2.5 완료 후 실행 가능 (T3.1 상태 확인 필요)
- T5.2 (SharePage 데이터 복원) — T2.5, T3.1, T3.2 완료 후 실행 가능
