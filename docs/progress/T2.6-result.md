## 구현 결과: T2.6 — useToast 훅

### 상태: OK

### 변경 파일
- `src/hooks/useToast.ts` (신규 생성): `useToast` 훅 export, `Toast` 인터페이스, `UseToastReturn` 인터페이스 export

### 구현 내용 요약
- `Toast` 인터페이스: `{ id: string; message: string }`
- `useToast()` 반환값: `{ toasts, showToast, removeToast }`
- `toasts: Toast[]` — 현재 활성 토스트 상태 배열
- `showToast(message: string)` — 고유 id(`Date.now() + random`) 생성 후 배열에 추가, 3초(3000ms) 후 `removeToast` 자동 호출
- `removeToast(id: string)` — id 일치 항목을 배열에서 제거
- `useCallback` 적용으로 불필요한 리렌더링 방지

### 핵심 제약사항 준수
- Arrow function 전용 사용 ✅
- 토스트 자동 사라짐 3초 (`setTimeout(..., 3000)`) ✅

### 생성된 테스트 파일
- 없음 (테스트 프레임워크 미설치 — package.json에 jest/vitest 없음, 스킵)

### 검증 결과
- 디자인 셀프체크: ⏭️ SKIP (비프론트엔드 로직)
- 새 테스트: ⏭️ SKIP (테스트 프레임워크 없음)
- 기존 테스트 회귀: ⏭️ SKIP
- TypeScript 타입 체크: ✅ PASS (tsc --noEmit 오류 없음)

### 다음 태스크
- T3.3 (Toast 컴포넌트) — T2.6 완료 후 실행 가능
