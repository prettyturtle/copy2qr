## 구현 결과: T3.1 Button 컴포넌트

### 상태: OK

### 변경 파일
- `src/components/ui/Button.tsx`: Button 컴포넌트 신규 생성. variant(primary/secondary/ghost), size(sm/md/lg), disabled 지원. primary는 보라→파랑 그라데이션(accent-violet → accent-blue) + glow shadow. secondary는 border 아웃라인 스타일. ghost는 투명 배경 + 호버 시 미세 fill. `ButtonHTMLAttributes<HTMLButtonElement>` spread로 onClick 등 native 속성 위임. arrow function, 시멘틱 `<button>` 태그.

### 생성된 테스트 파일
- 없음 (테스트 도구 미설치 — package.json에 vitest/jest 없음)

### 검증 결과
- 디자인 셀프체크: PASS (신규 폰트 도입 없음, cyan-on-dark/pure black 없음, bounce easing 없음)
- 새 테스트: SKIP (테스트 도구 미설치)
- 기존 테스트 회귀: SKIP (테스트 파일 없음)
- TypeScript tsc --noEmit: PASS (오류 없음)

### 호출처 영향
- 신규 생성 파일, 기존 호출처 없음
