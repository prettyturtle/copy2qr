## 구현 결과: T3.4 Modal 컴포넌트

### 상태: OK

### 변경 파일
- `src/components/ui/Modal.tsx` (생성): `Modal` 컴포넌트 구현. `isOpen`, `onClose`, `children` props. `bg-black/60` 오버레이. ESC 키 닫힘. 오버레이 클릭 닫힘. `isOpen` 시 `body.style.overflow = 'hidden'` 잠금. `createPortal(document.body)` 렌더링.

### 생성된 테스트 파일
- 없음 (plan에 별도 테스트 명시 없음)

### 검증 결과
- 디자인 셀프체크: 수정 0건 PASS (요구사항 명시 오버레이, glass 유틸리티 클래스 사용, bounce/elastic 없음)
- 새 테스트: SKIP (plan에 테스트 명시 없음)
- 기존 테스트 회귀: SKIP (관련 기존 테스트 없음)
- TypeScript 타입 체크: PASS (tsc --noEmit 에러 없음)

### 구현 상세
- `useEffect` #1: `isOpen` 변경 시 `document.body.style.overflow` 토글, cleanup으로 복원
- `useEffect` #2: `isOpen` true 시 `keydown` 리스너 등록, ESC 감지 시 `onClose()` 호출, cleanup 해제
- 오버레이 클릭: `e.target === overlayRef.current` 비교로 내부 콘텐츠 클릭 이벤트 버블링 차단
- `isOpen` false 시 `null` 반환 (조기 리턴, 포털 미생성)
- 콘텐츠: `glass` 유틸리티 클래스 + `animate-in fade-in zoom-in-95 duration-200`
- `role="dialog"` + `aria-modal="true"` 접근성 적용

### 다음 태스크
- T5.1 (ImageViewer 컴포넌트) — T3.4 의존 완료됨
