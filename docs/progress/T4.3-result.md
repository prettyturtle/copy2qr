## 구현 결과: T4.3 QRCodeDisplay 컴포넌트

### 상태: OK

### 변경 파일
- `src/components/QRCodeDisplay.tsx`: QRCodeDisplay 컴포넌트 생성. forwardRef + useImperativeHandle로 downloadQR 메서드 노출. QRCodeCanvas(fgColor="#1a1a2e", bgColor="#ffffff", level="M"). value 빈 값 시 빈 상태 렌더링.

### 생성된 테스트 파일
없음 (QRCodeCanvas는 canvas DOM 직접 조작으로 jsdom 환경 테스트 불가 — 실 브라우저 E2E에서 커버)

### 검증 결과
- 디자인 셀프체크: PASS (기존 프로젝트 토큰 시스템 사용, 신규 안티패턴 없음)
- 새 테스트: SKIP (테스트 미생성)
- 기존 테스트 회귀: SKIP (기존 테스트 없음)
- TypeScript: tsc --noEmit 통과 (오류 없음)

### 구현 상세
- Props: `value: string`, `size?: number` (기본 256)
- QRCodeCanvas ref: `canvasRef`로 canvas 참조, `canvas.toDataURL("image/png")`로 PNG 다운로드
- 다운로드: `a` 태그 동적 생성 → `a.click()` 트리거 → `qrcode.png`로 저장
- 외부 노출: `QRCodeDisplayHandle { downloadQR: () => void }` 타입으로 ref 노출
- 다크모드 대응: QR 자체는 흰 배경(#ffffff) + 어두운 전경(#1a1a2e) → 스캔 가능성 확보
- 빈 상태: value가 falsy일 때 안내 텍스트 표시

### 다음 태스크
- T4.4 (ActionBar): T3.1 완료 상태이므로 바로 실행 가능
- T4.5 (HomePage 통합): T4.1~T4.4 모두 완료 후 실행 가능
