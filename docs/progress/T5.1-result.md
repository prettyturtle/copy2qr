## 구현 결과: T5.1 — ImageViewer 컴포넌트

### 상태: OK

### 변경 파일
- `src/components/ImageViewer.tsx` (생성): Modal 래핑 이미지 뷰어. Props: src/isOpen/onClose. +/- 버튼으로 scale 0.5x~3x 조절, 초기화 버튼, 터치 핀치줌(touchstart/touchmove) 지원. 현재 배율 aria-live로 접근성 처리.

### 생성된 테스트 파일
- 없음 (Modal 래핑 + 상태 관리 위주 컴포넌트 — 브라우저 터치 이벤트 의존성으로 단위 테스트 효용 낮음)

### 검증 결과
- 디자인 셀프체크: PASS (기존 CSS 변수 토큰 사용, glass 유틸리티 미사용 — Modal 내부에서 glass 적용됨, 순수 검정/흰색 없음)
- 새 테스트: SKIP
- 기존 테스트 회귀: SKIP (기존 테스트 파일 없음)
- TypeScript tsc --noEmit: PASS
- Vite build: PASS (63 modules, 281.79 kB)

### 주요 구현 사항
- Arrow function 필수 준수
- CSS transform scale로 0.5x~3x 범위 줌 (SCALE_STEP=0.25)
- 핀치줌: touchstart 2-finger 거리 캡처 → touchmove에서 ratio 계산 → scale 업데이트
- 리셋 버튼: scale===1 일 때 disabled
- 시맨틱 HTML: button[type=button], aria-label, aria-live="polite" 배율 표시
